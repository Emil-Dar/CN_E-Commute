<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="10dp"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="5dp"
    android:backgroundTint="@color/white"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <!-- ================= HEADER ================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <TextView
                android:id="@+id/reportId"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Report ID: 0001"
                android:textStyle="bold"
                android:textColor="@color/primary_blue"
                android:textSize="14sp"
                android:layout_marginStart="4dp" />

            <TextView
                android:id="@+id/status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pending"
                android:textStyle="bold"
                android:textSize="12sp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:textColor="@color/white"
                android:background="@drawable/status_badge_bg"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="4dp" />

        </LinearLayout>

        <!-- ================= COMPLAINT MESSAGE ================= -->
        <TextView
            android:id="@+id/message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="A commuter reported a complaint. The PSTMU office is reviewing it."
            android:textSize="13sp"
            android:textColor="@color/dark_gray"
            android:layout_marginBottom="6dp" />

        <!-- ================= DETAILS SECTION ================= -->
        <LinearLayout
            android:id="@+id/detailsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/rounded_lightgray_bg"
            android:padding="8dp">

            <TextView
                android:id="@+id/detailsHeader"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Complaint Details"
                android:textStyle="bold"
                android:textColor="@color/primary_blue"
                android:textSize="13sp"
                android:layout_marginBottom="4dp" />

            <TextView
                android:id="@+id/violations"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Violation: Parking obstruction (6m from intersection)"
                android:textSize="13sp"
                android:textColor="@color/dark_gray"
                android:layout_marginBottom="4dp" />

            <TextView
                android:id="@+id/remarks"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Remarks: Scheduled for meeting"
                android:textSize="13sp"
                android:textColor="@color/medium_gray"
                android:layout_marginBottom="2dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/appointment"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Meeting scheduled on Oct 29, 2025 at 2:00 PM"
                android:textSize="12sp"
                android:textColor="@color/medium_gray"
                android:layout_marginBottom="4dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Filed on: Oct 27, 2025 11:15 PM"
                android:textSize="12sp"
                android:textColor="@color/medium_gray"
                android:layout_marginTop="2dp" />
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
